# Shared password for gated project pages.
# Used by /api/access to validate the password submitted through the gate form.
PAGE_ACCESS_PASSWORD=

# iron-session encryption secret. Minimum 32 characters.
# Generate one with: openssl rand -base64 32
SESSION_SECRET=
